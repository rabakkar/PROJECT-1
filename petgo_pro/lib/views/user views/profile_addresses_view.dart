import 'package:flutter/material.dart';

class ProfileAddressesView extends StatelessWidget {
  const ProfileAddressesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text("العناوين"),
    );
  }
}